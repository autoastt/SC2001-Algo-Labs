# SC2001

1. Create Python Virtual Environment
    ```bash
    python3 -m venv venv
    ```
2. Activate `venv`
    ```bash
    source venv/bin/activate 
    ```

3. Install Packages
    ```bash
    pip install -r requirements.txt
    ```
